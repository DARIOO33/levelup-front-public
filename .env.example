# Your backend API URL (proxied in next.config.mjs)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Google OAuth Client ID (public — safe to expose in browser)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
